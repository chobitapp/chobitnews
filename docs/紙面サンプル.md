# chobitnews 2026-09-21（品質検証用・手書き）

手元の使用バージョンは見ていない。Beat（個人 org 直近 90 日 + コミット割合 30% 以上）に載っているライブラリの、直近の上流だけを書いた。パイプラインは未使用。

採点は項ごと。読む / どうでもいい / 事実が違う。

## wrangler 4.135: Flagship をローカルで評価する

9月18日。開発時の Flagship バインディングが、既定で Miniflare のローカルストアを見る。本番フラグから切り離したままオフラインで動く。リモートを使うときはバインディングに `remote: true`。`wrangler flagship flags pull` でリモートから種をまける。

同じリリースで、実験的 Build Output を出すときに Dockerfile の Container イメージもビルドするようになった。

出典: https://github.com/cloudflare/workers-sdk/releases/tag/wrangler%404.135.0

## Vitest 5.0。計測の主眼は速さ

9月3日に 5.0、9月15日に 5.0.1。ブログの比較では Vitest 4.1.10 比で、依存の重いスイートが 1.59s → 0.74s（vmThreads）。Browser Mode と大きい isolated スイートが伸びる。環境起動が支配する `forks` + `jsdom` はほぼ横ばい。

5 は Vite >= 6.4 と Node >= 22.12 が要る。5.0.1 は UI のトレース表示と、list 失敗時の exit 1 など。

出典: https://vitest.dev/blog/vitest-5.html / https://github.com/vitest-dev/vitest/releases/tag/v5.0.1

## Zod 4.6: `.validate()` は結果を作らない

9月9日に 4.6.0、9月13日に 4.6.5。`.validate()` は「通るか」だけを返す。無効入力で `ZodError` を組まない。`z.compile()` と合わせると、無効パスで `.safeParse().success` より最大 35 倍速い、と公式が書いている。

ほかは `z.instanceof().properties()`（クラスインスタンスのプロパティをその場で見る）、`z.iban()`、再帰スキーマのメモリ保持の修正。4.6.4 で `z.url()` が `URL.canParse()` になり、無効 URL の拒否が約 50 倍速い。

出典: https://github.com/colinhacks/zod/releases/tag/v4.6.0 / https://zod.dev/blog/zod-4-6

## Hono 4.13.7: `hono/jsx` の XSS

9月4日。`Suspense` / `ErrorBoundary` / `Context.Provider` と、`hono/jsx/dom/server` の `renderToString` / `renderToReadableStream` が、子や fallback の素の文字列をエスケープせず出していた。[GHSA-hxh3-vqpv-xpqv](https://github.com/honojs/hono/security/advisories/GHSA-hxh3-vqpv-xpqv)。信頼できない文字列をそこに渡しているなら上げる話。

9月15日の 4.13.8 は JSX DOM の keyed update の高速化と Accept ヘッダの q=0 スキップなど。セキュリティではない。

出典: https://github.com/honojs/hono/releases/tag/v4.13.7 / https://github.com/honojs/hono/releases/tag/v4.13.8

## 今日書かないもの

- TypeScript 7.0（7月の正式リリース）。大きいが、今日の最新ではない
- Biome 2.5.14（9月16日）。バグ修正約 50 と nursery ルール 14。パッチの羅列なので hold
- Beat 上の `@types/*`、postcss、手元ピンの世代割れ
