# 同じ事実からの生成（2026-09-21）

手書き1号（[紙面サンプル.md](紙面サンプル.md)）の調査を JSON にし、本文だけを機械で書いた。Judgment（4本 print、TypeScript 7 と Biome は hold）は動かしていない。

事実は手書きから起こしている。近いのはそのためで、検証しているのは取材ではなく **調査済みの事実から項が書けるか**。

`pnpm verify:copy` がテンプレ。`tsx src/cli.ts copy --llm` が xAI（`XAI_API_KEY` + `grok-4.6`）。この検証時はキーが無かったので API は回していない。LLM 欄はこのセッションの grok-4.6 が、同じプロンプトと JSON だけを見て書いた。

## テンプレ（`pnpm verify:copy`）

```
# chobitnews 2026-09-21

## wrangler 4.135: Flagship をローカルで評価する
9月18日。開発時の Flagship バインディングが、既定で Miniflare のローカルストアを見る。本番フラグから切り離したままオフラインで動く。リモートを使うときはバインディングに remote: true。wrangler flagship flags pull でリモートから種をまける。同じリリースで、実験的 Build Output を出すときに Dockerfile の Container イメージもビルドする。

出典: https://github.com/cloudflare/workers-sdk/releases/tag/wrangler%404.135.0

## Vitest 5.0: 計測の主眼は速さ
9月3日。9月3日に 5.0、9月15日に 5.0.1。公式ブログの比較では Vitest 4.1.10 比で、依存の重いスイートが vmThreads で 1.59s から 0.74s。Browser Mode と大きい isolated スイートが伸びる。環境起動が支配する forks + jsdom はほぼ横ばい。Vite >= 6.4 と Node >= 22.12 が要る。5.0.1 は UI のトレース表示と、list 失敗時の exit 1 など。

出典: https://vitest.dev/blog/vitest-5.html / https://github.com/vitest-dev/vitest/releases/tag/v5.0.1

## Zod 4.6: `.validate()` は結果を作らない
9月9日。9月9日に 4.6.0、9月13日に 4.6.5。`.validate()` は通るかだけを返す。無効入力で ZodError を組まない。z.compile() と合わせると、無効パスで .safeParse().success より最大 35 倍速いと公式が書いている。z.instanceof().properties() はクラスインスタンスのプロパティをその場で見る。z.iban() と、再帰スキーマのメモリ保持の修正。4.6.4 で z.url() が URL.canParse() になり、無効 URL の拒否が約 50 倍速い。

出典: https://github.com/colinhacks/zod/releases/tag/v4.6.0 / https://zod.dev/blog/zod-4-6

## Hono 4.13.7: `hono/jsx` の XSS
9月4日。Suspense / ErrorBoundary / Context.Provider と、hono/jsx/dom/server の renderToString / renderToReadableStream が、子や fallback の素の文字列をエスケープせず出していた。GHSA-hxh3-vqpv-xpqv。信頼できない文字列をそこに渡しているなら上げる話。9月15日の 4.13.8 は JSX DOM の keyed update の高速化と Accept ヘッダの q=0 スキップなど。セキュリティではない。

出典: https://github.com/honojs/hono/releases/tag/v4.13.7 / https://github.com/honojs/hono/releases/tag/v4.13.8

## 今日書かないもの

- TypeScript 7.0: 7月の正式リリース。大きいが、今日の最新ではない
- Biome 2.5.14: バグ修正約 50 と nursery ルール 14。パッチの羅列なので hold
```

## LLM（セッション grok-4.6、事実 JSON のみ）

## wrangler 4.135: Flagship をローカルで評価する

9月18日。開発時の Flagship バインディングは、既定で Miniflare のローカルストアを見る。本番フラグから切り離したままオフラインで動く。リモートを使うときはバインディングに `remote: true`。`wrangler flagship flags pull` でリモートから種をまける。同じリリースで、実験的 Build Output を出すときに Dockerfile の Container イメージもビルドする。

出典: https://github.com/cloudflare/workers-sdk/releases/tag/wrangler%404.135.0

## Vitest 5.0: 計測の主眼は速さ

9月3日に 5.0、9月15日に 5.0.1。公式ブログの比較では Vitest 4.1.10 比で、依存の重いスイートが vmThreads で 1.59s から 0.74s。Browser Mode と大きい isolated スイートが伸び、環境起動が支配する forks + jsdom はほぼ横ばい。Vite >= 6.4 と Node >= 22.12 が要る。5.0.1 は UI のトレース表示と、list 失敗時の exit 1 など。

出典: https://vitest.dev/blog/vitest-5.html / https://github.com/vitest-dev/vitest/releases/tag/v5.0.1

## Zod 4.6: `.validate()` は結果を作らない

9月9日に 4.6.0、9月13日に 4.6.5。`.validate()` は通るかだけを返す。無効入力で ZodError を組まない。`z.compile()` と合わせると、無効パスで `.safeParse().success` より最大 35 倍速いと公式が書いている。ほかは `z.instanceof().properties()`（クラスインスタンスのプロパティをその場で見る）、`z.iban()`、再帰スキーマのメモリ保持の修正。4.6.4 で `z.url()` が `URL.canParse()` になり、無効 URL の拒否が約 50 倍速い。

出典: https://github.com/colinhacks/zod/releases/tag/v4.6.0 / https://zod.dev/blog/zod-4-6

## Hono 4.13.7: `hono/jsx` の XSS

9月4日。`Suspense` / `ErrorBoundary` / `Context.Provider` と、`hono/jsx/dom/server` の `renderToString` / `renderToReadableStream` が、子や fallback の素の文字列をエスケープせず出していた。GHSA-hxh3-vqpv-xpqv。信頼できない文字列をそこに渡しているなら上げる話。9月15日の 4.13.8 は JSX DOM の keyed update の高速化と Accept ヘッダの q=0 スキップなど。セキュリティではない。

出典: https://github.com/honojs/hono/releases/tag/v4.13.7 / https://github.com/honojs/hono/releases/tag/v4.13.8

## 比較

| | 手書き | テンプレ | LLM |
| --- | --- | --- | --- |
| 事実の過不足 | 正 | 同じ（入力が同じ） | 同じ |
| 日付の重複 | ない | Vitest / Zod で「9月3日。9月3日に」 | ない |
| リズム | 項ごとに緩急 | 点を `。` でつなぐだけ | 手書きに近い |
| 興味 | 通った | 届く。重複と羅列が欠点 | 手書きとほぼ同じ |

結論: 興味の本体は Investigation（どの点を残すか）と Judgment（何を hold するか）。本文はテンプレで届く。事実 JSON がすでに「一点」と箇条書きなら、LLM の差は日付の重複を消す程度。次に足りないのは、Releases からこの JSON を機械が切るところ。
